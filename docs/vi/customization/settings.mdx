---
title: Tổng quan Cài đặt
description: Cấu hình TablePro để phù hợp với quy trình làm việc và sở thích của bạn
---

# Tổng quan Cài đặt

TablePro cung cấp nhiều cài đặt để tùy chỉnh trải nghiệm của bạn. Truy cập cài đặt qua **TablePro** > **Settings** hoặc nhấn `Cmd+,`.

{/* Screenshot: Settings window showing all categories */}
<Frame caption="Cài đặt TablePro">
  <img
    className="block dark:hidden"
    src="/images/settings.png"
    alt="Cửa sổ cài đặt"
  />
  <img
    className="hidden dark:block"
    src="/images/settings-dark.png"
    alt="Cửa sổ cài đặt"
  />
</Frame>

## Danh mục Cài đặt

<CardGroup cols={2}>
  <Card title="Chung" icon="gear">
    Ngôn ngữ, hành vi khởi động, cập nhật phần mềm và các tùy chọn cơ bản
  </Card>
  <Card title="Giao diện" icon="palette" href="/vi/customization/appearance">
    Chủ đề, màu sắc và tùy chọn trực quan
  </Card>
  <Card title="Trình soạn thảo" icon="code" href="/vi/customization/editor-settings">
    Font, kích thước và hành vi của trình soạn thảo SQL
  </Card>
  <Card title="Bảng Dữ liệu" icon="table">
    Chiều cao hàng, định dạng ngày và hiển thị
  </Card>
  <Card title="Tab" icon="rectangle-on-rectangle">
    Hành vi tái sử dụng tab
  </Card>
  <Card title="Bàn phím" icon="keyboard" href="/vi/features/keyboard-shortcuts#tùy-chỉnh-phím-tắt">
    Tùy chỉnh phím tắt
  </Card>
  <Card title="AI" icon="sparkles" href="/vi/features/ai-chat">
    Cấu hình nhà cung cấp AI và cài đặt chat
  </Card>
  <Card title="Lịch sử" icon="clock-rotate-left">
    Cài đặt lưu giữ lịch sử truy vấn
  </Card>
  <Card title="Giấy phép" icon="key">
    Kích hoạt và quản lý giấy phép
  </Card>
</CardGroup>

## Cài đặt Chung

### Ngôn ngữ

Chọn ngôn ngữ hiển thị cho giao diện TablePro:

| Tùy chọn | Mô tả |
|-----------|-------|
| **Hệ thống** | Theo ngôn ngữ hệ thống macOS (mặc định) |
| **English** | Tiếng Anh |
| **Tiếng Việt** | Tiếng Việt |

<Note>
Thay đổi ngôn ngữ yêu cầu khởi động lại TablePro để có hiệu lực hoàn toàn.
</Note>

### Hành vi Khởi động

Kiểm soát điều gì xảy ra khi TablePro khởi chạy:

| Tùy chọn | Mô tả |
|--------|-------------|
| **Show Welcome Screen** | Hiển thị màn hình chào mừng với các kết nối gần đây |
| **Reopen Last Session** | Tự động kết nối lại với cơ sở dữ liệu cuối cùng của bạn |

### Thời Gian Chờ Thực Thi Truy Vấn

Đặt thời gian tối đa cho truy vấn chạy trước khi bị hủy:

| Cài đặt | Mặc định | Phạm vi | Mô tả |
|---------|---------|-------|-------------|
| **Query timeout** | 60 giây | 0 -- 600 | Số giây tối đa chờ truy vấn. Đặt 0 để không giới hạn. |

Thời gian chờ được thực thi ở cấp cơ sở dữ liệu để đảm bảo độ tin cậy tối đa:

| Cơ sở dữ liệu | Cơ chế |
|----------|-----------|
| PostgreSQL | `SET statement_timeout` (áp dụng cho tất cả câu lệnh) |
| MySQL | `SET SESSION max_execution_time` (áp dụng cho truy vấn SELECT) |
| MariaDB | `SET SESSION max_statement_time` (áp dụng cho truy vấn SELECT) |
| SQLite | `sqlite3_busy_timeout` (áp dụng cho xung đột khóa) |

<Note>
Thời gian chờ được áp dụng khi thiết lập kết nối mới. Thay đổi giá trị thời gian chờ cần kết nối lại để có hiệu lực.
</Note>

{/* Screenshot: Cài đặt thời gian chờ truy vấn */}
<Frame caption="Cấu hình thời gian chờ thực thi truy vấn">
  <img
    className="block dark:hidden"
    src="/images/settings-query-timeout.png"
    alt="Cài đặt thời gian chờ truy vấn"
  />
  <img
    className="hidden dark:block"
    src="/images/settings-query-timeout-dark.png"
    alt="Cài đặt thời gian chờ truy vấn"
  />
</Frame>

### Cập nhật Phần mềm

Kiểm soát cách TablePro kiểm tra và cài đặt cập nhật:

| Cài đặt | Mặc định | Mô tả |
|---------|---------|-------------|
| **Automatically check for updates** | Bật | Định kỳ kiểm tra phiên bản mới trong nền |
| **Check for Updates...** | — | Kiểm tra cập nhật thủ công ngay bây giờ |

TablePro sử dụng [Sparkle](https://sparkle-project.org/) để cập nhật an toàn và liền mạch. Khi có bản cập nhật, bạn sẽ thấy hộp thoại với ghi chú phát hành và tùy chọn cài đặt.

Bạn cũng có thể kiểm tra cập nhật từ menu **TablePro** > **Check for Updates...**.

### Quyền riêng tư

Kiểm soát việc thu thập dữ liệu sử dụng ẩn danh:

| Cài đặt | Mặc định | Mô tả |
|---------|---------|-------------|
| **Share anonymous usage data** | Bật | Gửi thống kê sử dụng ẩn danh (phiên bản OS, kiến trúc, ngôn ngữ, loại cơ sở dữ liệu) mỗi 24 giờ |

Dữ liệu thu thập hoàn toàn ẩn danh — không có thông tin cá nhân, truy vấn hoặc nội dung cơ sở dữ liệu nào được truyền đi. Bạn có thể tắt bất cứ lúc nào bằng cách tắt cài đặt này.

{/* Screenshot: General settings panel */}
<Frame caption="Cài đặt chung">
  <img
    className="block dark:hidden"
    src="/images/settings-general.png"
    alt="Cài đặt chung"
  />
  <img
    className="hidden dark:block"
    src="/images/settings-general-dark.png"
    alt="Cài đặt chung"
  />
</Frame>

## Cài đặt Bảng Dữ liệu

Cấu hình cách dữ liệu được hiển thị trong bảng kết quả.

### Chiều cao Hàng

| Tùy chọn | Chiều cao | Tốt nhất cho |
|--------|--------|----------|
| **Compact** | 20px | Dữ liệu dày đặc, xem nhiều hàng hơn |
| **Normal** | 24px | Chế độ xem cân bằng (mặc định) |
| **Comfortable** | 28px | Khả năng đọc tốt hơn |
| **Spacious** | 32px | Khả năng tiếp cận |

### Định dạng Ngày

Chọn cách hiển thị ngày:

| Định dạng | Ví dụ |
|--------|---------|
| ISO 8601 | 2024-12-31 23:59:59 |
| ISO Date | 2024-12-31 |
| US Long | 12/31/2024 11:59:59 PM |
| US Short | 12/31/2024 |
| EU Long | 31/12/2024 23:59:59 |
| EU Short | 31/12/2024 |

<Tip>
Định dạng ISO 8601 được khuyến nghị để đảm bảo tính nhất quán, đặc biệt khi làm việc với các nhóm quốc tế.
</Tip>

### Hiển thị NULL

Tùy chỉnh cách giá trị NULL xuất hiện:

| Cài đặt | Mặc định | Mô tả |
|---------|---------|-------------|
| **NULL Display** | "NULL" | Văn bản hiển thị cho giá trị NULL |

Giá trị hợp lệ:
- `NULL` - Ký hiệu cơ sở dữ liệu tiêu chuẩn
- `(null)` - Trong ngoặc đơn
- `∅` - Ký hiệu tập rỗng
- Văn bản tùy chỉnh (tối đa 20 ký tự)

### Kích thước Trang

Đặt số hàng mặc định được tải mỗi trang:

| Phạm vi | Tối thiểu | Tối đa | Mặc định |
|-------|---------|---------|---------|
| Kích thước Trang | 10 | 100,000 | 1,000 |

<Note>
Kích thước trang lớn hơn sử dụng nhiều bộ nhớ hơn nhưng hiển thị nhiều dữ liệu hơn cùng một lúc.
</Note>

<Note>
**Phạm vi xác thực:**
- **Kích thước Trang**: 10 -- 100,000 hàng (giá trị ngoài phạm vi này sẽ được giới hạn)
- **Hiển thị NULL**: Tối đa 20 ký tự, không cho phép xuống dòng hoặc tab
</Note>

### Màu Hàng Xen kẽ

| Tùy chọn | Mô tả |
|--------|-------------|
| **On** | Kẻ sọc zebra để quét dễ hơn |
| **Off** | Màu nền đồng nhất |

### Tự động Hiện Inspector

| Cài đặt | Mặc định | Mô tả |
|---------|---------|-------------|
| **Tự động hiện inspector khi chọn hàng** | Tắt | Tự động mở thanh bên phải khi chọn hàng trong tab bảng |

{/* Screenshot: Cài đặt Bảng Dữ liệu */}
<Frame caption="Cài đặt Bảng Dữ liệu: chiều cao hàng, định dạng ngày, hiển thị NULL">
  <img
    className="block dark:hidden"
    src="/images/settings-data-grid.png"
    alt="Cài đặt Bảng Dữ liệu"
  />
  <img
    className="hidden dark:block"
    src="/images/settings-data-grid-dark.png"
    alt="Cài đặt Bảng Dữ liệu"
  />
</Frame>

## Cài đặt Tab

Cấu hình hành vi tab khi mở bảng từ sidebar.

### Tái Sử Dụng Tab Bảng Sạch

| Cài đặt | Mặc định | Mô tả |
|---------|---------|-------------|
| **Reuse clean table tab** | Tắt | Khi bật, click vào bảng mới trong sidebar sẽ thay thế tab bảng hiện tại thay vì mở tab mới |

Một tab được coi là "sạch" khi tất cả các điều kiện sau đều đúng:

- Đó là tab bảng (không phải tab truy vấn hoặc tạo bảng)
- Không được ghim
- Không có thay đổi chưa lưu
- Bạn chưa tương tác với nó (chưa sắp xếp, lọc, chọn hàng, v.v.)

Khi cài đặt này tắt (mặc định), mỗi lần click bảng sẽ mở tab mới.

<Tip>
Bật tính năng này nếu bạn thích quy trình làm việc một tab khi duyệt bảng, tương tự hành vi mặc định của TablePlus.
</Tip>

{/* Screenshot: Cài đặt Tab */}
<Frame caption="Cài đặt Tab: hành vi tái sử dụng">
  <img
    className="block dark:hidden"
    src="/images/settings-tabs.png"
    alt="Cài đặt Tab"
  />
  <img
    className="hidden dark:block"
    src="/images/settings-tabs-dark.png"
    alt="Cài đặt Tab"
  />
</Frame>

## Cài đặt Bàn phím

Tùy chỉnh phím tắt cho các hành động menu. Xem [Phím tắt](/vi/features/keyboard-shortcuts#tùy-chỉnh-phím-tắt) để biết chi tiết đầy đủ.

### Gán lại Phím tắt

1. Mở **Settings** > **Keyboard**
2. Click vào ô phím tắt bên cạnh bất kỳ hành động nào
3. Nhấn tổ hợp phím mới
4. Phím tắt được cập nhật ngay lập tức

### Phát hiện Xung đột

Gán phím tắt đã được sử dụng sẽ hiển thị hộp thoại với tùy chọn **Cancel** hoặc **Reassign**. Các phím tắt hệ thống (`Cmd+Q`, `Cmd+H`, `Cmd+M`, `Cmd+,`) không thể gán lại.

### Đặt lại về Mặc định

Nhấp **Reset to Defaults** để khôi phục tất cả phím tắt về giá trị ban đầu.

{/* Screenshot: Cài đặt Keyboard */}
<Frame caption="Cài đặt bàn phím: tùy chỉnh phím tắt">
  <img
    className="block dark:hidden"
    src="/images/settings-keyboard.png"
    alt="Cài đặt bàn phím"
  />
  <img
    className="hidden dark:block"
    src="/images/settings-keyboard-dark.png"
    alt="Cài đặt bàn phím"
  />
</Frame>

## Cài đặt Lịch sử

Kiểm soát lưu giữ lịch sử truy vấn.

### Số Mục Tối đa

| Cài đặt | Mặc định | Mô tả |
|---------|---------|-------------|
| **Max Entries** | 10,000 | Số truy vấn tối đa được giữ |

Đặt thành `0` cho lịch sử không giới hạn.

### Số Ngày Tối đa

| Cài đặt | Mặc định | Mô tả |
|---------|---------|-------------|
| **Max Days** | 90 | Xóa các truy vấn cũ hơn số này |

Đặt thành `0` để giữ lịch sử mãi mãi.

### Tự động Dọn dẹp

| Tùy chọn | Mô tả |
|--------|-------------|
| **On** | Tự động xóa các mục cũ (mặc định) |
| **Off** | Chỉ dọn dẹp thủ công |

<Warning>
Tắt tự động dọn dẹp với các mục không giới hạn có thể khiến cơ sở dữ liệu lịch sử phát triển rất lớn theo thời gian.
</Warning>

### Xóa Lịch sử

Để xóa toàn bộ lịch sử truy vấn:

1. Mở Settings > History
2. Nhấp **Clear All History**
3. Xác nhận hành động

{/* Screenshot: Cài đặt Lịch sử */}
<Frame caption="Cài đặt Lịch sử: lưu giữ và dọn dẹp">
  <img
    className="block dark:hidden"
    src="/images/settings-history.png"
    alt="Cài đặt Lịch sử"
  />
  <img
    className="hidden dark:block"
    src="/images/settings-history-dark.png"
    alt="Cài đặt Lịch sử"
  />
</Frame>

## Cài đặt Giấy phép

Quản lý giấy phép TablePro từ tab **License** trong Settings.

### Kích hoạt Giấy phép

1. Mở **Settings** (`Cmd+,`) và chọn tab **License**
2. Nhập mã giấy phép vào ô văn bản
3. Nhấp **Activate**

Sau khi kích hoạt, tab hiển thị email đã đăng ký và mã giấy phép.

### Hủy kích hoạt Giấy phép

Để xóa giấy phép khỏi máy này:

1. Mở **Settings** > **License**
2. Nhấp **Deactivate...**
3. Xác nhận hành động

<Note>
Hủy kích hoạt sẽ giải phóng slot kích hoạt để bạn có thể sử dụng giấy phép trên máy khác.
</Note>

### Sử dụng Ngoại tuyến

TablePro xác thực giấy phép cục bộ bằng chữ ký mã hóa. Không cần kết nối internet cho việc sử dụng hàng ngày sau khi kích hoạt. Ứng dụng định kỳ xác thực lại với máy chủ (mỗi 7 ngày) để kiểm tra cập nhật.

{/* Screenshot: Cài đặt Giấy phép */}
<Frame caption="Kích hoạt và quản lý giấy phép">
  <img
    className="block dark:hidden"
    src="/images/settings-license.png"
    alt="Cài đặt Giấy phép"
  />
  <img
    className="hidden dark:block"
    src="/images/settings-license-dark.png"
    alt="Cài đặt Giấy phép"
  />
</Frame>

## Lưu trữ Cài đặt

Cài đặt được lưu trữ trong:

```
~/Library/Preferences/com.TablePro.plist
```

### Đặt lại Cài đặt

Để đặt lại tất cả cài đặt về mặc định:

```bash
# Xóa file preferences
rm ~/Library/Preferences/com.TablePro.plist

# Xóa cài đặt đã cache
rm -rf ~/Library/Caches/com.TablePro
```

Sau đó khởi động lại TablePro.

<Warning>
Đặt lại preferences sẽ không ảnh hưởng đến các kết nối đã lưu hoặc lịch sử truy vấn của bạn.
</Warning>

## Cài đặt theo Tính năng

### Liên quan đến Kết nối

| Cài đặt | Vị trí | Mô tả |
|---------|----------|-------------|
| Hành vi khởi động | General | Làm gì khi khởi chạy |
| Thời gian chờ truy vấn | General | Thời gian thực thi truy vấn tối đa |
| Tự động kiểm tra cập nhật | General | Tự động kiểm tra bản cập nhật |
| Tự động kết nối lại | Connections | Kết nối lại khi ngắt kết nối |
| Ngôn ngữ | Tổng quát | Ngôn ngữ hiển thị ứng dụng |
| Chia sẻ phân tích | General | Chia sẻ dữ liệu sử dụng ẩn danh |

### Liên quan đến Trình soạn thảo

| Cài đặt | Vị trí | Mô tả |
|---------|----------|-------------|
| Họ font | Editor | Lựa chọn font monospace |
| Kích thước font | Editor | Kích thước văn bản (11--18 pt, được giới hạn trong phạm vi) |
| Số dòng | Editor | Hiện/ẩn số dòng |
| Xuống dòng | Editor | Tự động xuống dòng dài |
| Độ rộng tab | Editor | Số khoảng trắng mỗi tab (1--16) |
| Chế độ Vim | Editor | Bật phím tắt Vim (chế độ Normal/Insert/Visual) trong trình soạn thảo SQL |

Xem [Cài đặt Trình soạn thảo](/vi/customization/editor-settings) để biết chi tiết.

### Liên quan đến Hiển thị

| Cài đặt | Vị trí | Mô tả |
|---------|----------|-------------|
| Chủ đề | Appearance | Light/Dark/System |
| Màu nhấn | Appearance | Màu nhấn UI |
| Chiều cao hàng | Data Grid | Chiều cao hàng bảng |
| Định dạng ngày | Data Grid | Định dạng hiển thị ngày |

Xem [Giao diện](/vi/customization/appearance) để biết chi tiết.

### Liên quan đến Bàn phím

| Cài đặt | Vị trí | Mô tả |
|---------|----------|-------------|
| Gán phím tắt | Keyboard | Tùy chỉnh phím tắt menu |
| Đặt lại về Mặc định | Keyboard | Khôi phục tất cả phím tắt về giá trị ban đầu |

### Liên quan đến Bộ lọc

| Cài đặt | Vị trí | Mô tả |
|---------|----------|-------------|
| Cột mặc định | Filters | Cột được chọn trước cho bộ lọc mới (Raw SQL, Primary Key, Any Column) |
| Toán tử mặc định | Filters | Toán tử được chọn trước cho bộ lọc mới |
| Trạng thái panel | Filters | Hành vi panel lọc khi mở (Hidden, Always Show, Restore Last) |

Xem [Lọc](/vi/features/filtering) để biết chi tiết.

## Phím tắt

Mở cài đặt nhanh chóng:

| Hành động | Phím tắt |
|--------|----------|
| Mở Settings | `Cmd+,` |

## Trang Liên quan

<CardGroup cols={2}>
  <Card title="Giao diện" icon="palette" href="/vi/customization/appearance">
    Cài đặt chủ đề và màu sắc
  </Card>
  <Card title="Cài đặt Trình soạn thảo" icon="code" href="/vi/customization/editor-settings">
    Cấu hình trình soạn thảo SQL
  </Card>
  <Card title="Lịch sử Truy vấn" icon="clock-rotate-left" href="/vi/features/query-history">
    Chi tiết tính năng lịch sử
  </Card>
  <Card title="Bảng Dữ liệu" icon="table" href="/vi/features/data-grid">
    Tính năng bảng dữ liệu
  </Card>
</CardGroup>
